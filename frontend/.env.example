# Supabase — safe to ship in the client. RLS is what protects the data.
VITE_SUPABASE_URL=https://naopagzyyhocsogtblab.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# NEVER put the service_role key in this file or anywhere in /frontend.
# It bypasses RLS entirely. It belongs only in Edge Function secrets.
